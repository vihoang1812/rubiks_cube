#ifndef COMMON_ALIAS
#define COMMON_ALIAS

#include <string>
#include <vector>
#include <queue>

template <class T>
using vec = std::vector<T>;

template <class T>
using mat = vec<vec<T>>;

template <class T>
using queue = std::queue<T>;

using uint = unsigned int;
using str = std::string;

#endif